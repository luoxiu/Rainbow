os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode10
env: SWIFT_VERSION=5.1
install:
  - if [[ $TRAVIS_OS_NAME == 'linux' ]]; then
      eval "$(curl -sL https://swiftenv.fuller.li/install.sh)";
    fi
script:
  - swift test
